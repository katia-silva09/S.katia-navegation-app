import { Text, View } from 'react-native';

const ProductsScreen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="font-work-black text-3xl">Productos</Text>
    </View>
  );
};

export default ProductsScreen;